{
  "name": "kindpos-licensing",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "db:init": "wrangler d1 execute kindpos-licenses --file=schema.sql",
    "db:init:remote": "wrangler d1 execute kindpos-licenses --remote --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
